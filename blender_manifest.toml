schema_version = "1.0.0"

# Example of manifest file for a Blender extension
# Change the values according to your extension
id = "painter3d"
version = "2.1.17"
name = "3DPainter"
tagline = "Layer-based hand-painting for Blender, forked from Paint System"
maintainer = "jinpyoung"
# Supported types: "add-on", "theme"
type = "add-on"

# Optional link to documentation, support, source files, etc
# website = "https://extensions.blender.org/add-ons/my-example-package/"

# Optional list defined by Blender and server, see:
# https://docs.blender.org/manual/en/dev/advanced/extensions/tags.html
tags = ["Paint"]

blender_version_min = "4.2.0"
# # Optional: Blender version that the extension does not support, earlier versions are supported.
# # This can be omitted and defined later on the extensions platform if an issue is found.
# blender_version_max = "5.1.0"

# License conforming to https://spdx.org/licenses/ (use "SPDX: prefix)
# https://docs.blender.org/manual/en/dev/advanced/extensions/licenses.html
license = [
  "SPDX:GPL-3.0-or-later",
]
# Optional: required by some licenses.
copyright = [
  "2026 jinpyoung (3DPainter fork)",
  "2024-2025 Tawan Sunflower",
  "2005-2025 Blender Authors (project edit/apply operators adapted from Blender's bl_operators/image.py)",
]

# Optional list of supported platforms. If omitted, the extension will be available in all operating systems.
# platforms = ["windows-x64", "macos-arm64", "linux-x64"]
# Other supported platforms: "windows-arm64", "macos-x64"

# Optional: bundle 3rd party Python modules.
# https://docs.blender.org/manual/en/dev/advanced/extensions/python_wheels.html
# platforms = ["linux-x64", "windows-x64", "macos-x64", "macos-arm64", "windows-arm64"]
# wheels = []




# Optional: add-ons can list which resources they will require:
# * files (for access of any filesystem operations)
# * network (for internet access)
# * clipboard (to read and/or write the system clipboard)
# * camera (to capture photos and videos)
# * microphone (to capture audio)
#
# If using network, remember to also check `bpy.app.online_access`
# https://docs.blender.org/manual/en/dev/advanced/extensions/addons.html#internet-access
#
# For each permission it is important to also specify the reason why it is required.
# Keep this a single short sentence without a period (.) at the end.
# For longer explanations use the documentation or detail page.
#
[permissions]
files = "Loading and saving images"
# clipboard = "Copy and paste brush colors"

# Optional: build settings.
# https://docs.blender.org/manual/en/dev/advanced/extensions/command_line_arguments.html#command-line-args-extension-build
# NOTE: defining [build] overrides Blender's built-in defaults, so the common
# excludes (__pycache__, .git, *.zip) are repeated here explicitly.
[build]
paths_exclude_pattern = [
  # Blender defaults
  "__pycache__/",
  "/.git/",
  "/*.zip",
  # Version control / CI / editor config
  "/.github/",
  "/.gitignore",
  "/.prettierrc",
  # Python dev environment / tooling (not needed to run the add-on)
  "/.venv/",
  "/.python-version",
  "/pyproject.toml",
  "/uv.lock",
  "/run_tests.py",
  # Runtime caches (regenerated on demand; avoid shipping stale data)
  "/donation_cache.json",
  "/version_cache.json",
  # Misc junk
  "*.py[cod]",
  ".DS_Store",
  "*.blend1",
  "scripts/",
  "dist/",
]
